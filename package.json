{
  "name": "scrubssitel", "version": "1.0.0", "private": true, "type": "module",
  "description": "An original, bilingual hospital management comedy game.",
  "scripts": { "dev": "node scripts/serve.mjs", "build": "node scripts/build.mjs", "test": "node --test tests/*.test.mjs", "check": "node --check src/game.js && node --check src/renderer.js && node --check src/main.js && npm test && npm run build" },
  "engines": { "node": ">=20" }
}
