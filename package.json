{
  "name": "scrubssitel",
  "version": "2.1.0",
  "private": true,
  "type": "module",
  "description": "An original, bilingual hospital management comedy game.",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check src/game.js && node --check src/renderer.js && node --check src/main.js && node --check src/sprites.js && node --check src/tutorial.js && node --check src/recruitment.js && npm test && npm run build"
  },
  "engines": {
    "node": ">=20"
  }
}
